services:
  - type: web
    name: cukcu
    env: python
    buildCommand: "pip install -r requirements.txt"    #"./build.sh
    startCommand: "python start.py"                    #"gunicorn cukcu.wsgi:appliocation"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: cukcu_db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: False
      - key: ALLOWED_HOSTS
        value: .onrender.com,cukcu.org
      - key: DJANGO_SETTINGS_MODULE
        value: CUKCU1.settings
    domains:
      - cukcu.org
      - www.cukcu.org

databases:
  - name: cukcu-db
    databaseName: cukcu
    plan: free